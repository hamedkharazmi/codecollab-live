# Docker Environment Variables
# PostgreSQL Configuration
POSTGRES_DB=codecollab
POSTGRES_USER=codecollab
POSTGRES_PASSWORD=codecollab_dev_password
POSTGRES_PORT=5432

# Backend Configuration
BACKEND_PORT=8000

# Frontend Configuration
FRONTEND_PORT=80

# Backend Internal Database URL (used by the backend service)
# This is automatically constructed from POSTGRES_* vars in docker-compose.yml
# but included here for reference
# DATABASE_URL=postgresql://codecollab:codecollab_dev_password@postgres:5432/codecollab
